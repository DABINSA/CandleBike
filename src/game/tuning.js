// 바이크 물리 튜닝 값 — 한 곳에 모아 두고 bike.js가 매 프레임 읽는다.
// ?tune=1 패널(src/tune.js)이 이 값을 실시간으로 바꿔 바로 체감 가능.
// 값이 확정되면 여기 기본값으로 박으면 됨.
export const TUNING = {
  // 구동
  torque: 0.42,        // 가속 토크(전진력)
  boostTorque: 0.16,   // 부스트 가산
  maxAv: 1.5,          // 바퀴 전진 각속도 상한(기본)
  boostAv: 2.2,        // 부스트 시 가산
  maxRev: 0.95,        // 후진 각속도 상한

  // 자세 제어(분리 컨트롤: 윌리/앞숙임)
  leanBack: 0.06,      // 앞들기(윌리) 토크
  leanFwd: 0.07,       // 앞숙임 토크

  // 자동 복원(용서) — 중립구간 밖에서만 천천히 지형에 정렬
  neutral: 0.22,       // 자유 제어 구간(±rad)
  recover: 0.7,        // 복원 강도
  recoverFlip: 1.2,    // 전복 직전 복원 강도
  flip: 0.8,           // 전복 임계(±rad)
  recoverCap: 0.6,     // 복원 각속도 상한
  maxSpin: 0.30,       // 차체 회전 각속도 상한(기본)
  boostSpin: 0.06,     // 부스트 시 가산

  // 점프
  jump: 11,            // 점프 임펄스
};
